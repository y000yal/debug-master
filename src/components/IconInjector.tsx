import React, { useEffect } from 'react';

export const IconInjector: React.FC = () => {
	useEffect( () => {
		// Phosphor icons are loaded via npm package, no injection needed.
	}, [] );

	return null;
};

